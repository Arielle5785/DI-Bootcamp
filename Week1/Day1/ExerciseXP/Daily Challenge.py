#1. string = input('Please enter a string of 10 characters exactly: ')
# length = len(string)
# if length < 10:
#     print (f'String not long enough. You have actually entered {length} number of characters.')
# elif length > 10:
#     print (f'String too long. You have actually entered {length} number of characters.')
# else:
#     print('perfect string')

#2.
# flc = input('enter a string of characters: ')
# first_c = flc[0]
# last_c = flc[-1]

# if len(flc) != 0:
#     print(f'first character is {first_c} and the last one is {last_c}.'  )

 