import { useSelector } from "react-redux";

const initialState = {
    tasks:[]
}
const TaskListSlice = createSlice(
    {
        name: 'task',
        initialState,
        date: tasks.date,
        reducers: {}
    }
    
)
export default TaskListSlice
