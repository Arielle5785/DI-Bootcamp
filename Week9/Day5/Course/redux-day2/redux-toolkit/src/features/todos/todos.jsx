import { addTask,removeTask,extraReducers } from "./TodoSlice";
const Todos = (props) => {
    return (
        <>
            <h2> To Do's List</h2>
            
        </>
    )
}

export default Todos;