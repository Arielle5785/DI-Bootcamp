class Car{
    constructor(public make: string, public model: string,public year: number, public isElectric: boolean) {
    const getCarDetails():(string |number| boolean) {return `My car is${make} is a model ${model} from year ${year}. Is it Electric? ${isElectric}`}
}
    }
    